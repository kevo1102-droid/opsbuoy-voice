# Exclude vendored third-party libraries from secret scanning.
# Minified library code triggers false positives on patterns like the
# HashiCorp Vault Service Token match against s.evaluateMemberExpression
# (transformers.js AST evaluator method calls).
paths-ignore:
  - "lib/**"
